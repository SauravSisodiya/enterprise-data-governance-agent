pandas>=2.0
numpy>=1.24

# demo_data.py reads the UCI Online Retail II dataset, distributed as .xlsx -
# pandas needs this installed to use its read_excel() openpyxl engine.
openpyxl>=3.1

# local .env loading for API keys during development
python-dotenv>=1.0

# policy ingestion + retrieval
pdfplumber>=0.10
fastembed>=0.3          # ONNX embeddings, no PyTorch install

# orchestration + surface
langgraph>=0.2
streamlit>=1.30

# local model client
#ollama>=0.3